# Infineon XMC1100 — XMC-Link / J-Link OB probe + bundled xmc1xxx flash driver
source [find interface/jlink.cfg]
transport select swd
adapter speed 1000
source [find target/xmc1xxx.cfg]
# XMC boards route no SRST line — reset via AIRCR.SYSRESETREQ only
cortex_m reset_config sysresetreq
